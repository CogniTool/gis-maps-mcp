{
  "name": "@mcp-marketplace/gis-maps",
  "version": "0.1.0",
  "description": "MCP Server: GIS & Maps — PostGIS spatial queries, geocoding, GeoJSON, map tiles, spatial analysis",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "test": "vitest"
  },
  "keywords": ["mcp", "gis", "postgis", "geojson", "maps", "spatial", "geocoding"],
  "author": "MCP Marketplace",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
